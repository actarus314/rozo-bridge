# Paths semgrep skips in this repository.
#
# Kept SEPARATE from the CI command (which comes from the template) so a future
# re-sync of ci.yml cannot silently drop these project-specific exclusions.

# Vendored third-party bundle — the Stellar Wallets Kit, regenerated by
# scripts/build-walletkit.mjs. Its findings (e.g. a postMessage target of "*") are
# upstream code, not this repo's to fix. Never scanned or edited (see AGENTS.md).
web/assets/walletkit.js

# Installed dependencies — present only in a local checkout, never scanned in CI.
node_modules/
