{
  "name": "rozo-bridge-devtools",
  "private": true,
  "version": "0.0.0",
  "description": "Dev-only tooling for this repo. The app itself (web/) has no build step and no runtime dependencies — this only regenerates the vendored web/assets/walletkit.js bundle.",
  "scripts": {
    "build:walletkit": "node scripts/build-walletkit.mjs"
  },
  "devDependencies": {
    "@creit.tech/stellar-wallets-kit": "2.5.0",
    "esbuild": "^0.28.1"
  },
  "overrides": {
    "protobufjs": "7.6.5"
  }
}
