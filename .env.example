# rozo-bridge — application 100 % statique (page + serve.py), servie sans build ni dépendance.
# AUCUNE variable de déploiement : pas de clé API, pas de backend, pas de secret runtime.
# Le PAT GitHub est un secret de DÉVELOPPEMENT → il vit dans .envrc (local), jamais ici.
