# rozo-bridge — 100% static app (page + serve.py), served without a build step or dependencies.
# NO deployment variables: no API key, no backend, no runtime secret.
# The GitHub PAT is a DEVELOPMENT secret → it lives in .envrc (local), never here.
